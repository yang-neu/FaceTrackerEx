#pragma once
#include <iostream>
#include <stdio.h>

using namespace std;

void movWindow2Center(string winName);
void getScreenCenter();
void openFileDialog(string winName);
string saveFileDialog(string winName);
string openFolderDialog(string );
std::string dirnameOf(const std::string& fname);